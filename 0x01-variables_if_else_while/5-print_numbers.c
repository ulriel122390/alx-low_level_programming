#include <stdio.h>
/**
 * main- where the code run
 *
 * return zero if no errors
 */

int main(void){

	int num;
	
	num = 0;
	while(num <= 9){
	
		printf("%d", num);
		num++;
	}
	printf("\n");
	return (0);
}
